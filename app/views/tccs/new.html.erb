<% content_for :title, "Novo TCC" %>
<% content_for :topbar_title, "Cadastrar Novo TCC" %>

<div class="page-header">
  <h1 class="page-title">Cadastrar TCC</h1>
  <%= link_to tccs_path, class: "btn-outline-custom" do %>
    <i class="bi bi-arrow-left"></i> Voltar
  <% end %>
</div>

<div class="row justify-content-center">
  <div class="col-lg-8">
    <div class="card">
      <div class="card-header">
        <i class="bi bi-file-earmark-plus me-2" style="color:#2F7AEA"></i>
        Informações do Trabalho
      </div>
      <div class="p-4">
        <%= form_with url: tccs_path, method: :post, multipart: true, local: true do |f| %>

          <!-- Erros da API -->
          <% if defined?(@erros) && @erros.any? %>
            <div class="flash-alert mb-3">
              <i class="bi bi-exclamation-triangle-fill"></i>
              <div>
                <strong>Corrija os erros:</strong>
                <ul class="mb-0 mt-1">
                  <% @erros.each do |campo, msgs| %>
                    <li><%= campo.capitalize %>: <%= Array(msgs).join(", ") %></li>
                  <% end %>
                </ul>
              </div>
            </div>
          <% end %>

          <!-- Título -->
          <div class="mb-3">
            <label class="form-label">Título <span class="text-danger">*</span></label>
            <%= f.text_field :titulo, class: "form-control", placeholder: "Título completo do trabalho",
                value: params[:titulo] %>
          </div>

          <!-- Resumo -->
          <div class="mb-3">
            <label class="form-label">Resumo</label>
            <%= f.text_area :resumo, class: "form-control", rows: 4,
                placeholder: "Breve descrição do trabalho…",
                value: params[:resumo] %>
          </div>

          <!-- Aluno + Orientador -->
          <div class="row g-3 mb-3">
            <div class="col-md-6">
              <label class="form-label">Aluno <span class="text-danger">*</span></label>
              <%= f.select :aluno_id,
                  options_for_select(
                    @alunos.map { |a| [a["nome"] || "#{a["first_name"]} #{a["last_name"]}".strip, a["id"]] },
                    params[:aluno_id]
                  ),
                  { prompt: "Selecione o aluno…" },
                  { class: "form-select" } %>
            </div>
            <div class="col-md-6">
              <label class="form-label">Orientador <span class="text-danger">*</span></label>
              <%= f.select :orientador_id,
                  options_for_select(
                    @professores.map { |p| [p["nome"] || "#{p["first_name"]} #{p["last_name"]}".strip, p["id"]] },
                    params[:orientador_id]
                  ),
                  { prompt: "Selecione o orientador…" },
                  { class: "form-select" } %>
            </div>
          </div>

        <!-- Curso + Status + Semestre -->
          <div class="row g-3 mb-3">
            <div class="col-md-5">
              <label class="form-label">Curso <span class="text-danger">*</span></label>
              <%= f.select :curso_id,
                  options_for_select(
                    @cursos.map { |c| [c["nome"], c["id"]] },
                    params[:curso_id]
                  ),
                  { prompt: "Selecione o curso…" },
                  { class: "form-select" } %>
            </div>
            <div class="col-md-4">
              <label class="form-label">Status</label>
              <%= f.select :status,
                  options_for_select([
                    ["Em Elaboração", "0"],
                    ["Enviado",       "1"],
                    ["Aprovado",      "2"],
                    ["Reprovado",     "3"]
                  ], params[:status] || "0"),
                  {},
                  { class: "form-select" } %>
            </div>
            <div class="col-md-3">
              <label class="form-label">Semestre da Defesa <span class="text-danger">*</span></label>
              <%= f.select :semester_letivo_defesa,
                  options_for_select([
                    "2020/1", "2020/2", 
                    "2021/1", "2021/2", 
                    "2022/1", "2022/2", 
                    "2023/1", "2023/2", 
                    "2024/1", "2024/2", 
                    "2025/1", "2025/2", 
                    "2026/1"
                  ], params[:tcc]&.[](:semestre_letivo_defesa) || "2026/1"),
                  { prompt: "Selecione o semestre…" },
                  { class: "form-select" } %>
            </div>
          </div>

          <!-- Palavras-chave + Tipo + Idioma -->
          <div class="row g-3 mb-3">
            <div class="col-md-4">
              <label class="form-label">Palavras-chave <span class="text-danger">*</span></label>
              <%= text_field_tag :palavras_chave, params[:palavras_chave], class: "form-control", placeholder: "Ex: rails, api, web" %>
            </div>
            <div class="col-md-4">
              <label class="form-label">Tipo <span class="text-danger">*</span></label>
              <%= select_tag :tipo, 
                  options_for_select([
                    ["Monografia", "MONOGRAFIA"],
                    ["Relatório de Estágio", "RELATORIO_ESTAGIO"],
                    ["Relatório Técnico", "RELATORIO_TECNICO"],
                    ["Artigo", "ARTIGO"]
                  ], params[:tipo]), 
                  { prompt: "Selecione…", class: "form-select" } %>
            </div>
            <div class="col-md-4">
              <label class="form-label">Idioma <span class="text-danger">*</span></label>
              <%= select_tag :idioma, 
                  options_for_select([
                    ["Português", "PT"],
                    ["Inglês", "EN"]
                  ], params[:idioma]), 
                  { prompt: "Selecione…", class: "form-select" } %>
            </div>
          </div>

          <!-- Banca Examinadora (Presidente + Membros) -->
          <div class="row g-3 mb-3">
            <div class="col-md-4">
              <label class="form-label">Presidente da Banca <span class="text-danger">*</span></label>
              <%= select_tag :presidente_id,
                  options_for_select(@professores.map { |p| [p["nome"] || "#{p["first_name"]} #{p["last_name"]}".strip, p["id"]] }, params[:presidente_id]),
                  { prompt: "Selecione…", class: "form-select" } %>
            </div>
            <div class="col-md-4">
              <label class="form-label">Primeiro Membro <span class="text-danger">*</span></label>
              <%= select_tag :primeiro_membro_id,
                  options_for_select(@professores.map { |p| [p["nome"] || "#{p["first_name"]} #{p["last_name"]}".strip, p["id"]] }, params[:primeiro_membro_id]),
                  { prompt: "Selecione…", class: "form-select" } %>
            </div>
            <div class="col-md-4">
              <label class="form-label">Segundo Membro <span class="text-danger">*</span></label>
              <%= select_tag :segundo_membro_id,
                  options_for_select(@professores.map { |p| [p["nome"] || "#{p["first_name"]} #{p["last_name"]}".strip, p["id"]] }, params[:segundo_membro_id]),
                  { prompt: "Selecione…", class: "form-select" } %>
            </div>
          </div>

          <!-- Upload de arquivo PDF -->
          <div class="mb-4">
            <label class="form-label">Arquivo PDF</label>
            <div class="file-upload-area" id="uploadArea">
              <input type="file" name="arquivo" accept=".pdf" id="pdfInput">
              <i class="bi bi-cloud-arrow-up-fill" style="color:#2F7AEA"></i>
              <p class="file-name">Clique ou arraste o PDF aqui</p>
              <p style="color:#94A3B8; font-size:.72rem">Somente arquivos .pdf</p>
            </div>
          </div>

          <!-- Botões -->
          <div class="d-flex gap-2 justify-content-end">
            <%= link_to tccs_path, class: "btn-outline-custom" do %>
              Cancelar
            <% end %>
            <button type="submit" class="btn-primary-custom">
              <i class="bi bi-save me-1"></i> Cadastrar TCC
            </button>
          </div>

        <% end %>
      </div>
    </div>
  </div>
</div>
